const db = require("../db");
const fetch = require("node-fetch");
const { post, comment } = require("./helpers/dummies");

describe("Comments", () => {
  test("Delete a comment", async () => {});
});
